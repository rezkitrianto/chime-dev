beam=10.0 # beam for decoding.  Was 13.0 in the scripts.
lattice_beam=5.0 # this has most effect on size of the lattices.
max_active=8000

#original setting. beam=16; lattice_beam=8; max_active=16000
